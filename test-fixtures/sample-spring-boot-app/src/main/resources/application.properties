# Bound to AppProperties (@ConfigurationProperties(prefix = "shop")) and read via
# @Value in AppConfig. The app is never run; these exist so the property keys are
# real. Properties are not bytecode, so they do not appear in CodeLens scans.
shop.catalog-url=https://catalog.example.com
shop.page-size=25
